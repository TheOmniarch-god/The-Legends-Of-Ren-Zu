# Codex UI Polish: Count + Label Fix

Changed file:

- `index.html`

## What changed

- Renamed `Public Rank` to simply `Rank`.
- Removed confusing path ratios from filter chips and path cards.
  - Path chips now show total Gu count only.
  - Path cards now show `13 Gu`, `11 Gu`, `8 Gu` instead of `5/13` style fractions.
- Path cards now use clearer text:
  - `2 refined · 1 trace`
  - `Path complete`
- Added `Showing X Gu` above the Codex grid so the number of cards in the current view is explicit.
- Removed the phrase `Public Gu ranks` from Codex explanation.

## Upload

Upload only:

- `index.html`
