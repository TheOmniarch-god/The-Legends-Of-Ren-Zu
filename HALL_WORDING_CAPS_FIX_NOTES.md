# Hall Wording + Nav Capitalization Fix

Changed file:

- `index.html`

## What changed

- Nav label now reads exactly: `Hall Of Venerables`.
- Replaced colder/public-facing phrases:
  - `Public listing` -> `Hall listing`
  - `public frame` -> `Hall frame`
  - `Public appearance` -> `Visibility`
  - `public roll` -> `roll`
- Kept opt-in meaning, but made it feel less corporate/legal in the UI.

## Upload

Upload only:

- `index.html`
