# Hall Copy + Nav Correction

Changed file:

- `index.html`

## What changed

- Removed frontend copy that sounded like the app was personally talking to the developer/user.
- Hall language is now public/product-facing.
- Removed implementation/back-end phrasing from the Hall UI.
- Nav menu label is now `Hall of Venerables` instead of just `Hall`.
- Nav menu was widened slightly so the Hall label fits.

## Upload

Upload only:

- `index.html`
